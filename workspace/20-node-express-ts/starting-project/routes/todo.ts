// LESSON 251: ROUTES MODULE (SCAFFOLD).
// Separating routing code from server bootstrap and from data access is a
// common Express convention: it keeps `app.ts` small and makes each route
// file responsible for exactly one resource. This file is the future home of
// the todo-related HTTP endpoints (GET /todos, POST /todos, etc.). The
// current lesson only creates the file so the project structure is ready;
// the actual handlers are added in later lessons.
export {};
